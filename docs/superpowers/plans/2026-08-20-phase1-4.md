# vet402 Phase 1–4 — 可能な全range実装計画（2026-08-20夜・ノンストップ実行）

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans。
> 正本: `~/Downloads/vet402_Technical_Implementation_Specification_v1.md` §4 Phase 1–4

**Goal:** コードで完結できる範囲のPhase 1–4を本番着地させる。資金投入・外部送信・契約はフラグOFF実装＋承認キュー起票で境界に置く。

**実測済みの前提（このセッションで一次確認）:**
- Solanaカタログ218件・network=`solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp`・チャレンジはx402 v2同形（palmyr.ai実取得）
- **catalog-syncがpayToを無条件小文字化→base58破壊**。原本は`raw_accepts`に生存
- ERC-8004実装はidentity/reputationのみ（validation registryなし）・x402-payerはBase専用ゲート
- cronは全て日次のAPIルート（vercel.json）・CRON_SECRET auth
- issued_at migrationは適用済み（本セッションで実行・TODO#1消込）

## 承認境界（実装するがONにしない／起票のみ）
- Solana L1実購入: 実装＋テスト・`OBSERVATORY_SOLANA_L1_ENABLED`未設定=OFF（ウォレット資金はTakeshi承認）
- ERC-8004 Validation書込/Reputation還流: 実装・`REGISTRY_WRITES_ENABLED`未設定=OFF（ガス代）
- 外部セキュリティレビュー委託・x402.org掲載PR・売り手アウトリーチ: **文面起票のみ**（外部送信/課金）
- npm新パッケージ公開: しない（コードはexamples/に置く）

## タスク列（各タスク: テスト→実装→緑→コミット）

### P1-1 メトリクス履歴（仕様1.1）
- Drizzle新テーブル `x402_daily_metrics`(date, chain, l0_probes, l0_pass, l1_attempts, l1_settled, spent_units, PK(date,chain))
- `src/lib/observatory/metrics-rollup.ts` rollupDailyMetrics(date)——raw2テーブルから冪等upsert。TDD(DB-backed)
- `scripts/backfill-daily-metrics.ts`（全既往日）
- cron `/api/cron/metrics-rollup`（日次・既存cron雛形踏襲）+ vercel.json（日次厳守=Hobby制約）
- reader `getDailyMetricsHistory(days)` + `GET /api/v1/observatory/history`（openapi追従・IP RL）
- `/observatory/state` に履歴チャート（インラインSVG・紙面様式・依存追加なし）
- 本番 db:push（preflight経由）→ backfill実行 → 本番APIで履歴実測

### P1-2 Solana/SOL-402（仕様1.2）
1. **取込修正**: catalog-syncのpayTo正規化をnetwork-aware化（eip155のみ小文字化）。既往218件の修復スクリプト（raw_acceptsから復元・本番SELECT→UPDATE）
2. **sol402-payer.ts**: parse/selectはx402-payer共通形を再利用しnetworkゲートだけ差替。支払いペイロードは x402 spec のSolana scheme実物仕様をWebFetchで確認してから実装（想像で書かない）。@solana/web3.js系dep追加。TDD（palmyr実チャレンジをフィクスチャに）
3. **l1-runner分岐**: candidate.networkでpayer選択。Solanaは `OBSERVATORY_SOLANA_L1_ENABLED`+鍵の二重ゲート・予算は同一台帳（USDC units共通）
4. ドキュメント: ARCHITECTURE英日にChainAdapter節追記

### P1-3 オンチェーン検証レジストリ（仕様1.3・フラグOFF）
- `src/lib/chain/registry.ts`: ERC-8004 Validation Registryへの(endpoint, level, result, evidenceHash)書込。viem walletClient・冪等（`registry_writes`台帳）・`REGISTRY_WRITES_ENABLED`ゲート・ガスサーキットブレーカ
- ABI/アドレスはERC-8004実仕様から。Base上に既設が無ければ最小デプロイscriptを用意しAQ起票（デプロイはガス=承認後）
- Solana側レジストリ: 設計文書のみ（program deployは資金・承認後）

### P1-4 トラクション種まき（仕様1.4・起票のみ）
- 上位実需要セラー5件への検証パートナー打診文面（英語）→ approval-queue.md へAQ起票
- x402.org/ecosystem掲載PR文面（WO項5と統合）→ 同上

### P2-4 Farcaster Frame（仕様2.4）
- `/api/frame/endpoint/[id]`——fc:frame meta＋既存バッジSVG画像＋endpointページへのリンク。テスト＋openapi

### P2-2 フレームワークアダプタ（仕様2.2・委任可）
- `examples/langchain-tool/`（DynamicStructuredToolでSDKラップ・dry-run実走）
- `examples/elizaos-plugin/`（プラグイン形・dry-run実走）
- 既存 `examples/agentkit-spend-guard` READMEにCDP facilitator/paymaster注記（2.1の文書分）

### P2-3 評判還流（仕様2.3・フラグOFF）
- settle後にERC-8004 reputationへ結果を還流する `feedOutcomeToReputation`——registry.tsと同フラグ・重み/減衰は設定値

### P3-3 許可不要L0コントリビューション v0（仕様3.3・フラグOFF）
- `probe_contributions`テーブル＋`POST /api/v1/observatory/contributions`（EIP-191署名検証・保存のみ・公開verdictへは**混ぜない**）・`CONTRIBUTIONS_ENABLED`ゲート・RL

### P4 経済化・コンプライアンス（文書＋軽実装）
- `GET /api/v1/observatory/export.csv`——購入台帳のCSVエクスポート（機関向け監査证跡v0・RL付き）
- `docs/open-core.md`（OSS/専有境界の明文化）・`docs/economic-capture-design.md`・`docs/tee-zk-integrity.md`（3.2設計）——委任可
- AQ起票: 外部セキュリティレビュー委託（3.1）

### 仕上げ
- 全テスト・typecheck・`npm run build`実走・mainマージ・push・本番実測（history API/state チャート/frame）
- 決定台帳記帳・WO/TODO消込・最終報告

## やらないこと（正直に）
- Solana実購入のON（資金）・レジストリ書込のON（ガス）・npm publish・外部送信の実行・Optimism拡張（カタログにOP行が実在しない）・ML予測モデル（3.4は既存台帳+文書まで）・マルチリージョン/SLA（4.3）
